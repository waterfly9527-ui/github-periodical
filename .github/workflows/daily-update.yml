name: Manual Periodical Placeholder

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Note manual trigger
        run: |
          echo "This workflow is manual-only. Scheduled periodical publishing is handled by the ChatGPT automation."
